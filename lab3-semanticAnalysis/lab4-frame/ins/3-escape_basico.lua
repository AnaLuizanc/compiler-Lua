function calculaArea(base:int, altura:int):int
    local area:int
    local multiplicador:int
    multiplicador = 1
    base = 5
    altura = 10
    area = externa(base, altura) * multiplicador
    return area
end